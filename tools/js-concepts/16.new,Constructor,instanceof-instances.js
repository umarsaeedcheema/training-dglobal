function Student (name, age){
    this.name = name 
    this.age = age
}
umar = new Student( 'Umar', 22)
console.log(umar.name, umar.age)

// new creates a new object 
// all the objects have an internal property called 'prototype'


// type of is used to check the value if an element is of a primitive type 

// instanceof checks if a value is an instance of a class or a constructor function.